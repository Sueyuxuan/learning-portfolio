# Day 12-2026-7-23-Python-Learning

## 1.今天学了什么

\-这节课的前情提要说，如果你之前学过其他编程语言，比如c++、java...可能会觉得python稍微没有那么复杂，比如在设置变量前不需要说明变量种类。

* ### Function calls函数调用
- print 是一个 Python 函数，用于在屏幕上显示传递给它的值。调用函数的方法是在函数名后加上括号，并在括号中放置函数的输入（或参数）。
* ### Comments 注释
- 用于理解代码，以#开头，于代码前单开一行。//day4提到过
* 等一下学到这里我突然发现好多知识点都是之前学过的，从这节课开始应该就步入实践阶段了。

* | Operator | Name           | Description                                      |
| -------- | -------------- | ------------------------------------------------ |
| a + b    | Addition       | Sum of a and b                                    |
| a - b    | Subtraction    | Difference of a and b                             |
| a \* b    | Multiplication | Product of a and b                                |
| a / b    | True division  | Quotient of a and b                               |
| a // b   | Floor division | Quotient of a and b, removing fractional parts     |
| a % b    | Modulus        | Integer remainder after division of a by b         |
| a \*\* b   | Exponentiation | a raised to the power of b                        |
| -a       | Negation       | The negative of a                                 |

- Python里有两种除号，“/”和“//”。前者就是很简单的除法，后者是在得到商后向下取整。
- 计算顺序在day4也提到过，就是正常的数学计算顺序。
- max取最大值，min最小，abs绝对值。
- 除了表示数据类型，int和float也可以作为函数调用，向下取整和化为浮点数（可以理解为+.0）。

## 2.有什么问题
- 我有点不明白为什么我的表格打不出来，自己按照教程打的和复制的都没法渲染成表格。
- 感觉对于基础知识点掌握的还可以，这节课投入实践后基本没有之前学过但遗忘or不认识的。
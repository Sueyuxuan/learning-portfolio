# From Kaggle Courses to Research: My Python Learning Reflection

> 从 Kaggle 入门课程走向科研：我的 Python 阶段性学习总结
>
> 阶段：完成 Kaggle *Intro to Programming*、*Python*、*Pandas* 三门入门课程之后
> 定位：工具准备阶段的总结，不是能力认证
> 更新时间：2026.8.18

这篇文章不是三门课程的知识点汇总，而是我对自己**思维方式和实际能力发生了什么变化**的一次记录。写下来的目的，是让我自己清楚：我现在站在什么位置，还缺什么，以及接下来该怎么走。

---

## 1. Why I Learned These Three Courses

我学这三门课，最初的动机并不是"我想学一门编程语言"。

我接下来准备做经济学 / 数据分析方向的研究。虽然具体题目还在摸索中，但我大致能预见到，真正开始做的时候我会遇到这样一些事情：

- 拿到的是 CSV 或 Excel 格式的原始数据；
- 数据往往跨越多个年份，甚至多个国家或地区；
- 一个研究问题通常需要同时看多个指标；
- 原始数据不会直接可用，需要清洗；
- 需要按条件筛选出我真正关心的那部分观测；
- 不同指标可能来自不同数据源，需要合并；
- 需要做描述性统计，先看清楚数据长什么样；
- 需要做趋势分析，看变量随时间怎么变化；
- 需要画图，把结果表达出来；
- 最后可能还要做一些相对简单的统计分析。

这些事情里，没有一件是"编程题"。它们都是**研究任务**。但我很快意识到一个现实问题：在能够处理这些任务之前，我连"把一个 CSV 文件读进来并看看它有几行几列"都做不到。

所以对我来说，这三门 Kaggle 课程的定位很清楚：

> **它们是我的科研工具准备阶段，而不是学习目标本身。**

我不是为了拿三张 certificate，而是为了让自己具备"能开始动手碰真实数据"的最低门槛。这个区别决定了我怎么看待自己现在的水平——我获得的是入场资格，不是研究能力。

---

## 2. What Each Course Helped Me Understand

我不打算按"这门课教了 A、B、C"来写。我更想回答的是：**每一门课让我跨过了什么门槛？**

### 2.1 Intro to Programming

这门课最大的作用，是让我第一次真正建立了"程序是如何按步骤执行"的概念。

课程内容本身涉及 variables、data types、functions、conditions 这些基础，但对我来说重要的不是这些词的定义，而是它们背后那个思维方式的转变：

**在此之前**，我对"用电脑处理数据"的想法大概是：我想要一个结果，然后去找一个功能、一个按钮、一个菜单项，让它给我这个结果。我关心的是"哪里能点出来"。

**在此之后**，我开始意识到：计算机不理解"我想要什么"，它只能执行"我告诉它的每一步"。如果我想让它做一件事，我必须先把这件事拆开，明确说清楚：

- 输入是什么？
- 规则是什么？
- 在什么条件下走哪条分支？
- 输出应该是什么形式？

这听起来很基础，但对我而言这是一次真实的认知变化。它其实和科研里的**问题拆解**是同一件事：一个模糊的研究想法（"我想看看某个政策有没有效果"）本身是无法执行的，必须被拆成一系列具体、明确、可操作的步骤，才有可能真的做下去。

我现在会这样理解：**编程训练的不只是写代码的能力，还有把一个模糊问题拆成明确步骤的习惯。**

### 2.2 Python

如果说 Intro to Programming 让我"看懂程序在干什么"，那 Python 这门课让我开始能够**自己写一点东西去解决问题**。

课程覆盖了 variables、functions、lists、loops、conditionals、strings、external libraries。但我想记下来的是这些东西各自意味着什么：

**Lists**
让我第一次理解"批量"这个概念。数据不是一个一个孤立的值，而是可以被组织成一个集合，整体保存、整体处理。这是从"一个数"到"一组数"的跨越，而研究里我面对的永远是"一组数"。

**Loops**
这是对我冲击比较大的一个。以前我默认重复的工作就是要重复地做——比如在 Excel 里对 20 个年份重复同一套操作。循环让我意识到：**重复任务不需要人来重复，只要规则一致，就可以交给程序按规则自动执行。** 这直接改变了我对"工作量"的估计方式。

**Functions**
让我理解到，一段会被反复用到的逻辑可以被封装起来，起个名字，需要的时候调用，而不是不断复制粘贴。更重要的是，封装之后如果发现逻辑有问题，我只需要改一个地方，而不是回头找出所有复制过的位置。这对后面做数据清洗其实很关键——清洗规则改一次，全部结果跟着更新。

**External libraries**
这是我第一次意识到 Python 真正的价值之一：**很多复杂任务根本不需要我从零写。** 有人已经写好了成熟的工具，我要做的是知道它存在、知道它能干什么、知道怎么正确地调用它。对一个初学者来说，这其实是个解脱——我不需要先成为程序员，才能开始处理数据。

### 2.3 Pandas

这是三门课里和我后续研究关系最直接的一门，也是我认为自己收获最具体的一门。

Pandas 让我第一次接触到一件事：**一张真实的数据表格，是可以通过代码来操作的。**

在这之前，我脑子里处理表格的方式是 Excel 式的——选中、拖动、排序、筛选、手动删掉几行。Pandas 提供的是另一套思路：每一步操作都是一条明确的、可以被记录、可以被重新运行的指令。

课程涉及的内容包括 DataFrame 和 Series 的结构、selecting、indexing、filtering、summary functions、groupby、sorting、missing values、renaming、combining data。我想按"它对应什么研究任务"来记录：

| Pandas 操作 | 我预计它在研究中对应什么 |
| --- | --- |
| DataFrame / Series | 理解一张数据表的基本结构：行是观测，列是变量 |
| Selecting / Indexing | 从一堆变量里取出我真正关心的那几个指标 |
| **Filtering** | 筛选某一地区、某一时间段，或满足特定条件的样本 |
| Summary functions | 先看清楚数据的量级、分布和基本特征，再谈分析 |
| **GroupBy** | 按年份、地区或类别做汇总和比较——这是很多经济学分析的基本动作 |
| Sorting | 找出最高 / 最低值，观察排序后有没有异常的观测 |
| **Missing values** | 意识到真实数据不像练习数据那样完整，缺失本身就是一种信息 |
| Renaming | 不同数据源的变量名往往不一致，需要统一之后才能对得上 |
| **Combining data** | 不同指标常常来自不同来源，必须合并才能一起分析 |

其中对我思维影响最大的是 **missing values** 和 **combining data** 这两块。

缺失值让我第一次意识到：课程练习里的数据是被整理过的，而真实世界的数据是有缺口的。而且缺失不是随机的噪音——某个国家某几年没有数据，这件事本身可能就说明了什么（统计能力、政治变动、口径调整）。**缺失值是数据质量问题，不是一个用 `fillna()` 就能消掉的技术细节。**

合并数据则让我意识到，研究中很少有"一个文件解决所有问题"的情况。我关心的变量 A 在这个数据库，变量 B 在另一个数据库，它们的国家名称写法可能不一样，年份覆盖范围可能不一样，统计口径可能也不一样。合并这个动作本身，是需要判断的。

---

## 3. What I Can Do Now

这一部分我不按课程分，而是按**能力**分。我尽量写得保守一些。

### 3.1 基础程序逻辑

我现在能够看懂并理解简单 Python 程序在做什么，包括：

- variables：变量是什么、赋值是怎么回事；
- conditions：`if` / `else` 的分支逻辑；
- loops：循环在重复什么、重复多少次；
- functions：一个函数接受什么、返回什么。

我不能说自己能设计复杂程序，但我能读懂一段简单代码的流程。

### 3.2 基础代码阅读能力

这一条我想单独强调，因为它在现在这个时间点特别重要。

即使一段代码不是完全由我独立写出来的（很多时候是参考课程示例，或者由 AI 生成），我现在应该能够：

- 阅读简单代码，大致看懂它在干什么；
- 判断它的整体逻辑走向；
- 修改其中的参数（比如换一个列名、改一个筛选条件、调整一个阈值）；
- 发现一部分明显的错误（比如列名写错、筛选条件方向反了）；
- 理解 AI 生成代码的大致结构，而不是把它当作一个黑盒直接运行。

**在 AI 可以随时生成代码的时代，"读懂并检查代码"这个能力的重要性，我认为已经超过了"从零默写代码"。** 

### 3.3 基础数据处理能力

我现在能用 Pandas 完成一些基本操作：

- 导入数据（`pd.read_csv()`）；
- 查看数据的基本情况（`head()`、`shape`、`info()`）；
- 选择需要的变量（列选择）；
- 按条件筛选数据；
- 排序；
- 计算一些简单的统计量（均值、最大值、计数等）；
- 用 `groupby()` 做分组汇总；
- 处理一部分缺失值；
- 合并结构比较简单的数据表。

需要说明的是：**这些操作我能在"数据比较干净、任务比较明确"的情况下完成。** 一旦数据变复杂，我大概率还是会卡住。

### 3.4 初步的数据思维

这是我认为三门课里最有价值、但最不容易被"证书"体现出来的收获。

我开始意识到：在写任何一行分析代码之前，应该先问自己一串问题：

- **一行代表什么？** 是一个国家？一个国家—年份？一个人？一次交易？
- **一列代表什么？** 这个变量的确切定义是什么？
- **数据单位是什么？** 是元还是万元？是绝对值还是百分比？是名义值还是实际值？
- **时间范围是什么？** 覆盖哪些年份？中间有没有断档？
- **有没有缺失值？** 缺在哪里？是随机缺失还是系统性缺失？
- **有没有重复值？** 同一个观测是不是被记录了两次？
- **不同文件中的变量能不能直接合并？** 口径一致吗？地区划分一致吗？年份定义一致吗？

我越来越觉得，**这些问题比"会不会写代码"更重要**。代码写错了通常会报错，或者结果明显不对；但如果这些前提问题没想清楚，代码可能顺利跑完，输出一个看起来很漂亮但完全没有意义的结果。

---

## 4. More Important Than Coding: What I Learned About Data

这三门课表面上教的是语法，但它们真正让我改变的，是我对"数据"这个东西的态度。

**之前**，我下意识地把数据当成"已经存在的、客观的、可以直接拿来用的事实"。

**现在**，我开始把数据看成"某个人、某个机构，在某个时间、按照某种口径、出于某种目的记录下来的结果"。这意味着数据是有来历的，有边界的，也是可能出错的。

我开始注意到几件事：

1. **数据是被生产出来的，不是天然存在的。** 一个统计指标背后有定义、有统计方法、有可能的口径变更。
2. **完整的数据是例外，不是常态。** 练习数据的完整是被人为处理过的。
3. **两张表能不能合并，是一个需要论证的判断，不是一个技术操作。** 名称能对上不代表含义能对上。
4. **数据处理的每一步都在改变结果。** 筛掉一部分样本、填补一个缺失值、换一种汇总方式，都会影响最后的结论。

这一层认识，我觉得比任何一个具体函数都更值得记下来。

---

## 5. What I Still Cannot Do

写这一节的目的是让我自己保持清醒。完成三门入门课之后，我仍然缺很多东西：

- **还不能独立完成一个完整的科研项目。** 从提出问题到得出结论的整条链条，我只碰过其中很小的一段。
- **对复杂 Python 代码的理解仍然有限。** 稍微长一点、嵌套多一点、用到不熟悉库的代码，我读起来就很吃力。
- **调试（debug）能力很弱。** 遇到报错时，我经常只能靠搜索或者问 AI，还不太能自己定位问题出在哪一步。
- **真实数据清洗经验几乎为零。** 我处理过的都是课程准备好的数据，没有面对过格式混乱、编码错误、单位不统一的原始文件。
- **统计方法掌握不足。** 我能算均值、能分组汇总，但对回归、显著性、识别策略这些，我目前的理解还非常表面。
- **没有真正处理过复杂的、多来源的数据。** 课程里的 merge 是干净的，现实里的 merge 不是。
- **不会做复杂的数据可视化。** 我目前对画图的理解还停留在"能画出来"，而不是"画得清楚、画得诚实"。
- **不能把相关关系解释成因果关系**——更准确地说，我知道不该这么做，但我还没有掌握那些让因果推断成立的方法和条件。
- **对研究设计、变量定义、数据质量判断，仍然需要系统学习。**

所以我想明确写下来：

> **完成 Kaggle 入门课，只是让我获得了进入真实项目的最低工具基础，而不是意味着研究能力已经形成。**

---

## 6. The Gap Between Kaggle Exercises and Real Research

这一节是我目前想得最多的一部分。

Kaggle 课程里的数据和任务，通常具备这样几个特征：

- 数据**已经被整理好**了：格式规范、编码统一、没有奇怪的空行；
- 变量含义**相对清晰**：列名可读，单位明确；
- 任务**已经被定义好**了：题目会告诉我"请筛选出满足某条件的行"；
- 答案**相对明确**：有标准答案，做对了就通过。

这套设置对学语法非常有效——它把干扰因素都去掉了，让我能专注在操作本身。但它也让我在一段时间里产生了一个错觉：**数据分析就是执行一系列已经被定义好的操作。**

真实研究不是这样的。真实研究里，最难的部分恰恰是那些课程替我省略掉的环节：

1. **我到底要研究什么问题？** 这个问题足够具体吗？值得研究吗？
2. **什么数据能回答这个问题？** 我关心的概念，有没有对应的可测量指标？
3. **数据从哪里获得？** 官方统计？数据库？公开数据集？各自的局限是什么？
4. **数据是否可信？** 谁统计的？出于什么目的？有没有已知的质量问题？
5. **不同数据的口径是否一致？** 同一个"GDP"，不同来源可能算法不同。
6. **缺失值怎么处理？** 删掉？填补？还是保留并说明？这个选择会不会改变结论？
7. **异常值是真实现象还是数据错误？** 一个特别高的值，可能是录入错误，也可能正是我要研究的现象。
8. **如何避免过度解释结果？** 我看到了一个相关关系，我到底能说到什么程度？

这些问题没有一个是 Pandas 能回答的。

所以我现在的理解是：

> **学习 Python 教会我"如何操作数据"；接下来的科研训练，要教我"为什么这样处理数据"。**

前者是技能，后者是判断。而判断只能在真实项目里练出来。

---

## 7. Why Python Still Matters in the Age of AI

这是我在学习过程中反复问自己的一个问题：既然 ChatGPT、Claude、Codex 都能几秒钟生成一段 Pandas 代码，我花时间学这些还有意义吗？

我不想用"AI 不能取代人"这种空泛的话来回答。我想具体一点。

**AI 确实能做很多事**，而且做得很好：

- 生成代码；
- 解释一段我看不懂的代码；
- 按我的要求修改代码；
- 帮我 debug；
- 大幅提高效率。

在我自己的学习过程中，这些帮助是真实的。

**但问题在于：AI 生成的代码在语法上几乎总是对的，在研究意义上却未必对。** 而后者，AI 不知道，因为它不了解我的研究问题、我的数据来历、我的变量定义。

如果我完全不懂 Python，我很难判断：

- 这段代码是不是真的在回答我的研究问题；
- 它有没有选错列（比如我要的是人均 GDP，它取了 GDP 总量）；
- 它的筛选条件有没有搞反（`>` 写成了 `<`，或者年份区间少了一年）；
- 单位有没有处理错（把万元当成元来加总）；
- `merge` 有没有造成行数膨胀（一对多匹配导致观测被重复计算）；
- 缺失值是不是被不恰当地填补了（用均值填补一个本来就系统性缺失的变量）；
- 最后输出的这个数字，到底有没有意义。

这些错误的共同特点是：**代码不会报错，程序会正常运行，结果会正常输出。** 只有理解逻辑的人才能发现它们。

所以我给自己的结论不是"我要做到不用 AI 独立写出所有代码"——那既不现实，也没必要。我真正需要培养的能力是：

> **能够理解代码逻辑、检查 AI 的输出、发现其中的错误、提出更好的问题，并最终对研究结果负责。**

代码可以是 AI 写的，但结论是我署名的。这个责任没法外包。

---

## 8. Before vs After

我尽量克制地总结一下变化：

| Before | After |
| --- | --- |
| 把 Python 看成"一门编程语言" | 开始把 Python 看成一个研究工具 |
| 看到代码就有点发怵，倾向于跳过 | 能读懂简单代码的基本结构 |
| Excel 式的手工处理思维（选中、拖动、手改） | 开始理解"可重复的数据处理流程"是什么意思 |
| 不理解 DataFrame 是什么 | 能理解行、列、变量、筛选之间的关系 |
| AI 给了代码就直接运行 | 开始尝试先读一遍，检查逻辑再运行 |
| 默认数据是干净、完整、可用的 | 开始预期数据是有缺口、有问题、需要检查的 |
| 关注"这段代码怎么写" | 开始关注"这一步处理会不会改变结论" |

这些变化都是**方向上的**，不是程度上的。我并没有从"不会"变成"会"，而是从"没有意识"变成"有了意识"。真正的熟练还差得很远。

---

## 9. From Kaggle Skills to Research Tasks

我把课程里学到的具体操作，和我预期它们在研究中的用途做了一个对应表。这也是我给自己准备的一份"迁移清单"：

| Kaggle Skill | Future Research Use（预期用途） |
| --- | --- |
| `pd.read_csv()` | 导入统计数据文件，作为所有分析的起点 |
| `.head()` | 快速检查数据结构：列名对不对，前几行长什么样 |
| `.shape` | 确认行数和列数，尤其是在 merge 前后对比是否异常 |
| `.info()` | 检查变量类型与缺失情况，判断数据是否需要清洗 |
| `.describe()` | 初步查看数值分布，发现明显异常的量级 |
| filtering（布尔筛选） | 筛选研究所需的时间范围、地区范围或特定条件的样本 |
| `.groupby()` | 按年份 / 地区 / 类别汇总，做横向和纵向比较 |
| `merge()` | 合并来自不同来源的指标，构建研究数据集 |
| missing values 处理 | 数据质量检查，判断缺失的模式和处理方式 |
| `.sort_values()` | 排序观察极值，识别可能的异常观测 |
| summary statistics | 做初步描述性统计，形成对数据的基本认识 |
| `.rename()` | 统一不同来源数据的变量命名，为合并做准备 |
| functions | 把重复的清洗逻辑封装起来，保证处理方式一致 |
| loops | 对多个年份 / 多个文件执行同一套处理流程 |

写这张表的时候我发现一件事：**每一个技术操作，在研究语境下都对应着一个判断。** 比如 `merge` 不只是"把两张表拼起来"，而是"我认为这两组数据描述的是同一批对象"。这个判断可能是错的。

---

## 10. My Minimum Python Capability at This Stage

我想非常谨慎地描述自己现在的水平。我不会写 "Proficient in Python"，因为那不是事实。

我目前可以合理地说，自己具备以下**最小化能力**：

- **Basic Python literacy** — 能读懂基本的 Python 语法结构，理解简单程序的执行流程。
- **Basic data manipulation with Pandas** — 能对结构清晰的表格数据完成导入、查看、选择、筛选、排序、分组汇总等基本操作。
- **Ability to read and modify simple Python code** — 能读懂并修改简单代码，调整参数，发现部分明显错误。
- **Ability to use AI assistance more critically** — 能在使用 AI 生成代码时，做初步的逻辑检查，而不是完全被动接受。
- **Ability to begin working with real tabular datasets** — 具备了开始尝试接触真实数据的最低条件。

需要强调最后一条里的 **begin**。

> 这些能力的意义在于：**它们是研究项目的起点，不是研究能力的证明。** 我现在能做的，是"开始"，而不是"完成"。

---

## 11. My Next Step: Learning Through Research

三门课程已经结束。我认为**下一步不应该是继续无限地刷 Python 教程**，因为再多的教程也不会教我怎么面对一份真实且不完美的数据。

下一步应该是：**Learn by doing research.**

我给自己设计了一条从 Kaggle 迁移到实际研究的路径：

### Stage 1 — Inspect Real Data（读真实数据）

先不做任何分析，只做一件事：把一份真实数据读进来，然后看清楚它是什么。

重点练习：
- `head()` — 看前几行
- `shape` — 看规模
- `columns` — 看有哪些变量
- `dtypes` — 看变量类型对不对
- `info()` — 看缺失情况
- `describe()` — 看数值分布

**目标：能够用自己的话说清楚"这份数据的一行代表什么"。**

### Stage 2 — Clean Data（清洗数据）

在真实数据上练习处理：
- missing values（缺失值：分布在哪里，怎么处理，为什么这样处理）
- duplicate rows（重复观测）
- data types（类型错误，比如数字被读成文本）
- inconsistent names（地区名、变量名不统一）
- units（单位不一致）
- years（年份格式、覆盖范围、口径变化）

**目标：每一步清洗都能说明理由，而不是"因为报错所以改"。**

### Stage 3 — Build a Research Dataset（构建研究数据集）

把来自不同来源的数据整理成一张可以直接分析的表。

这一步要特别注意 merge 前后的行数变化，以及匹配失败的观测是哪些、为什么失败。

**目标：产出一份自己能解释清楚每一列来历的数据集。**

### Stage 4 — Exploratory Data Analysis（探索性分析）

- descriptive statistics（描述性统计）
- trend analysis（趋势分析）
- simple plots（简单可视化）
- comparisons（分组比较）

**目标：先看清楚数据说了什么，再考虑它意味着什么。**

### Stage 5 — Ask Research Questions（提出研究问题）

从：

> "这张图是什么？"

转向：

> "这个现象为什么会出现？"
> "数据能支持多强的结论？"
> "还有什么别的解释？"

**目标：从数据操作者，慢慢变成提问者。**

## 12. My Python Learning Principles Going Forward

给自己定几条原则，避免走回"无限刷教程"的老路：

**1. Research first, Python second.**
先明确研究问题，再去学解决这个问题所需要的 Python。技术服务于问题，不是反过来。

**2. Learn when needed.**
遇到真实问题时再补充对应知识。不为了"完整"而提前学一堆用不上的东西。

**3. Understand before running.**
AI 给出的代码，先读懂再运行。至少要能说出它每一步在做什么。

**4. Check every transformation.**
每一步数据处理之后都检查结果——行数变了吗？有没有产生意料之外的缺失？分布还合理吗？

**5. Simple code is enough.**
现阶段优先使用简单、我自己能看懂的代码。看不懂的"优雅写法"对我来说风险大于收益。

**6. Reproducibility matters.**
整个数据处理过程应该可以从原始数据重新跑一遍得到同样的结果。不做无法追溯的手工修改。

---

## 13. Reflection

刚开始的时候，我把学 Python 理解成一件很直接的事：**学一门编程语言。** 学会语法，学会函数，能写出运行不报错的代码，就算学会了。

但完成这三门课之后，我的理解变了。

对我来说，Python 更重要的意义并不在编程本身，而在于：**它让我能够把一个现实问题，转化成一个可以被检查、被整理、被分析的数据过程。**

这句话里的"可以被检查"，是我现在觉得最关键的部分。手工在 Excel 里改数据，改完就没有痕迹了，别人无法复核，几个月后连我自己都想不起来当时为什么那样改。而写成代码之后，每一步都留在那里，可以被重新运行，可以被质疑，也可以被纠正。这本身就是一种研究态度。

同时我也在调整对自己的要求。

我接下来的目标，不是继续收集更多的课程证书。多修几门课很容易，也很有成就感，但那种成就感是虚的——它不会让我在面对一份真实数据时更有底气。真正需要做的，是把已经学到的这些基础，实实在在地放进一个科研项目里，然后在里面暴露出我所有不会的地方。

所以我想把自己的检验标准从：

> "我完成了多少门课程？"

换成：

> **"当我面对一份真实且不完美的数据时，我是否知道第一步应该做什么？"**

按这个标准衡量，我现在的答案还很模糊。但至少我已经知道该问什么问题了，而在三门课之前，我连问题都提不出来。

> **Completing these three Kaggle courses is not the end of my Python learning. It is the point where tutorial-based learning begins to turn into research-based learning.**

---

## 相关笔记 / Related Notes

我在学习过程中整理的笔记已经上传在本仓库。


